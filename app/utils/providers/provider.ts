// Legacy normalizeEmail stub (see constants shim). Scheduled for removal after cache purge.
export function normalizeEmail(email: string) { return email.toLowerCase() }
/**
 * Deprecated provider abstraction stub.
 * All OAuth/provider flows have been removed (Phase 0 scope change -> Phase 1 cleanup).
 * This file remains temporarily to avoid large cascading diff; any importers should be removed.
 */
export {}
