// Deprecated GitHub provider stub intentionally left empty (Phase 1 cleanup).
export {}
/**
 * Deprecated GitHub OAuth provider implementation (fully removed scope).
 * Kept as an empty module stub so imports break loudly once references are purged.
 * Phase 1 task will remove this file entirely after dependency removal.
 */
export {}
