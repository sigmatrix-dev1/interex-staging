// Intentionally blank legacy stub to satisfy React Router typegen for a previously removed route.
// Do not import or use. This exists solely to avoid generated type errors referencing this path.
// Export a trivial component so the file is considered a module by TypeScript/typegen.
export default function RemovedLegacyRoute() {
	return null
}
